/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['localhost', 'api.ayberkenis.com.tr', '*.ayberkenis.com.tr', 'restcountries.com'],
    },
};

export default nextConfig;
